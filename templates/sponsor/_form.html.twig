{% set route = app.request.attributes.get('_route') %}

  

{{ form_start(form, {'attr':{'class':'form-material'}})}}

<div class="form-field">
    {{form_label(form.nom)}}
    {{form_widget(form.nom, {'attr':{'class':'form-control'}})}}
</div>

<div class="form-field">
    {{form_label(form.image)}}
    {{form_widget(form.image, {'attr':{'class':'form-control'}})}}
</div>
<div class="form-field">
    {{form_label(form.description)}}
    {{form_widget(form.description, {'attr':{'class':'form-control'}})}}
</div>

    <div class="mt-3">
        {% if route =="sponsor_new" %}
                <button class="btn btn-update shadow-1 primary rounded-4 btn-large primary mr-2">Ajouter</button>
        {% elseif route =="sponsor_edit"%}
                <button href="{{ path('sponsor_edit', {'id': sponsor.id}) }}" class=" btn btn-edit  shadow-1 primary rounded-4 btn-large primary mr-2">Modifier</button>
        {% endif %}
        
        <div style="float: right;">
            <a href="{{ path('sponsor') }}" class="btn btn-list shadow-1 primary rounded-4 btn-large primary mr-2">Revenir à la liste</a>
        </div>
    </div>
{{ form_end(form) }}

